<!-- Copyright 2026 bitserv-ai -->
diff --git a/common/arg.cpp b/common/arg.cpp
index fdf58614b..5b4f06c19 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -1445,6 +1445,27 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             params.swa_full = true;
         }
     ).set_env("LLAMA_ARG_SWA_FULL"));
+    add_opt(common_arg(
+        {"--ser", "--smart-expert-reduction"}, "Kmin,thresh",
+        string_format("Smart Expert Reduction: always select Kmin experts, prune others below thresh*max_prob (default: %d,%.2f = disabled)",
+            params.min_experts, (double) params.thresh_experts),
+        [](common_params & params, const std::string & value) {
+            auto pos = value.find(',');
+            if (pos == std::string::npos) {
+                throw std::invalid_argument("expected Kmin,thresh format for --ser");
+            }
+            int kmin = std::stoi(value.substr(0, pos));
+            float thresh = std::stof(value.substr(pos + 1));
+            if (kmin < 1) {
+                throw std::invalid_argument("Kmin must be >= 1");
+            }
+            if (thresh < 0.0f) {
+                throw std::invalid_argument("threshold must be >= 0.0");
+            }
+            params.min_experts    = kmin;
+            params.thresh_experts = thresh;
+        }
+    ));
     add_opt(common_arg(
         {"-ctxcp", "--ctx-checkpoints", "--swa-checkpoints"}, "N",
         string_format("max number of context checkpoints to create per slot (default: %d)"
diff --git a/common/common.cpp b/common/common.cpp
index 0dd9ede5e..99b0fe85b 100644
--- a/common/common.cpp
+++ b/common/common.cpp
@@ -1601,6 +1601,9 @@ struct llama_context_params common_context_params_to_llama(const common_params &
     cparams.type_k = params.cache_type_k;
     cparams.type_v = params.cache_type_v;
 
+    cparams.min_experts    = params.min_experts;
+    cparams.thresh_experts = params.thresh_experts;
+
     return cparams;
 }
 
diff --git a/common/common.h b/common/common.h
index 2adb310b8..9e54e73f1 100644
--- a/common/common.h
+++ b/common/common.h
@@ -455,6 +455,8 @@ struct common_params {
     int32_t n_parallel            =     1; // number of parallel sequences to decode
     int32_t n_sequences           =     1; // number of sequences to decode
     int32_t n_outputs_max         =     0; // max outputs in a batch (0 = n_batch)
+    int32_t min_experts           =    -1;
+    float   thresh_experts        =  0.0f;
     int32_t grp_attn_n            =     1; // group-attention factor
     int32_t grp_attn_w            =   512; // group-attention width
     int32_t n_print               =    -1; // print token count every n tokens (-1 = disabled)
