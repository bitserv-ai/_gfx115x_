<!-- Copyright 2026 bitserv-ai -->
diff --git a/ggml/src/ggml-cpu/ops.cpp b/ggml/src/ggml-cpu/ops.cpp
index c555831ce..9253c73c7 100644
--- a/ggml/src/ggml-cpu/ops.cpp
+++ b/ggml/src/ggml-cpu/ops.cpp
@@ -742,7 +742,10 @@ static void ggml_compute_forward_add_id_f32(
         // src1 indices
         const int i11 = *(int32_t *) ((char *) src2->data + i1*nb20 + i2*nb21);
 
-        GGML_ASSERT(i11 >= 0 && i11 < ne11);
+        if (i11 < 0 || i11 >= ne11) {
+            memset((char *) dst->data + i3*nb3 + i2*nb2 + i1*nb1, 0, ne0 * sizeof(float));
+            continue;
+        }
 
         ggml_vec_add_f32(ne0,
                 (float *) ((char *) dst->data  + i3*nb3  + i2*nb2  + i1*nb1 ),
@@ -4815,7 +4818,10 @@ static void ggml_compute_forward_get_rows_q(
         const int64_t i10 = (i - i12*ne11*ne10 - i11*ne10);
         const int64_t i01 = *(int32_t *) ((char *) src1->data + i10*nb10 + i11*nb11 + i12*nb12);
 
-        GGML_ASSERT(i01 >= 0 && i01 < ne01);
+        if (i01 < 0 || i01 >= ne01) {
+            memset((char *) dst->data + i10*nb1 + i11*nb2 + i12*nb3, 0, nc * ggml_type_size(dst->type));
+            continue;
+        }
 
         dequantize_row_q(
                 (const void *) ((char *) src0->data + i01*nb01 + i11*nb02 + i12*nb03),
@@ -4856,7 +4862,10 @@ static void ggml_compute_forward_get_rows_f16(
         const int64_t i10 = (i - i12*ne11*ne10 - i11*ne10);
         const int64_t i01 = *(int32_t *) ((char *) src1->data + i10*nb10 + i11*nb11 + i12*nb12);
 
-        GGML_ASSERT(i01 >= 0 && i01 < ne01);
+        if (i01 < 0 || i01 >= ne01) {
+            memset((char *) dst->data + i10*nb1 + i11*nb2 + i12*nb3, 0, nc * ggml_type_size(dst->type));
+            continue;
+        }
 
         ggml_cpu_fp16_to_fp32(
             (const ggml_fp16_t*) ((char *) src0->data + i01*nb01 + i11*nb02 + i12*nb03),
@@ -4897,7 +4906,10 @@ static void ggml_compute_forward_get_rows_bf16(
         const int64_t i10 = (i - i12*ne11*ne10 - i11*ne10);
         const int64_t i01 = *(int32_t *) ((char *) src1->data + i10*nb10 + i11*nb11 + i12*nb12);
 
-        GGML_ASSERT(i01 >= 0 && i01 < ne01);
+        if (i01 < 0 || i01 >= ne01) {
+            memset((char *) dst->data + i10*nb1 + i11*nb2 + i12*nb3, 0, nc * ggml_type_size(dst->type));
+            continue;
+        }
 
         ggml_cpu_bf16_to_fp32(
             (const ggml_bf16_t *) ((char *) src0->data + i01*nb01 + i11*nb02 + i12*nb03),
@@ -4938,7 +4950,10 @@ static void ggml_compute_forward_get_rows_f32(
         const int64_t i10 = (i - i12*ne11*ne10 - i11*ne10);
         const int64_t i01 = *(int32_t *) ((char *) src1->data + i10*nb10 + i11*nb11 + i12*nb12);
 
-        GGML_ASSERT(i01 >= 0 && i01 < ne01);
+        if (i01 < 0 || i01 >= ne01) {
+            memset((char *) dst->data + i10*nb1 + i11*nb2 + i12*nb3, 0, nc * ggml_type_size(dst->type));
+            continue;
+        }
 
         ggml_vec_cpy_f32(nc,
                 (float *) ((char *)  dst->data + i10*nb1  + i11*nb2  + i12*nb3),
diff --git a/ggml/src/ggml-vulkan/ggml-vulkan.cpp b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
index c0ab9f1c6..49b2e8a49 100644
--- a/ggml/src/ggml-vulkan/ggml-vulkan.cpp
+++ b/ggml/src/ggml-vulkan/ggml-vulkan.cpp
@@ -1158,6 +1158,7 @@ struct vk_mat_vec_id_push_constants {
     uint32_t ne11;
     uint32_t expert_i1;
     uint32_t nbi1;
+    uint32_t n_experts;
 };
 
 struct vk_flash_attn_push_constants {
@@ -1435,6 +1436,8 @@ struct vk_op_topk_moe_push_constants {
     uint32_t with_norm;
     float output_scale;
     float output_bias;
+    float ser_thresh;
+    uint32_t ser_min_experts;
 };
 
 struct vk_op_add_id_push_constants {
@@ -1444,6 +1447,7 @@ struct vk_op_add_id_push_constants {
     uint32_t s02;
     uint32_t s11;
     uint32_t s21;
+    uint32_t n_experts;
 };
 
 struct vk_op_diag_mask_push_constants {
@@ -9864,7 +9868,7 @@ static void ggml_vk_mul_mat_vec_id_q_f16(ggml_backend_vk_context * ctx, vk_conte
 
     const uint64_t ne00 = src0->ne[0];
     const uint64_t ne01 = src0->ne[1];
-    // const uint64_t ne02 = src0->ne[2];
+    const uint64_t ne02 = src0->ne[2];
     // const uint64_t ne03 = src0->ne[3];
 
     const uint64_t ne10 = src1->ne[0];
@@ -10062,7 +10066,8 @@ static void ggml_vk_mul_mat_vec_id_q_f16(ggml_backend_vk_context * ctx, vk_conte
             (uint32_t)ne00, (uint32_t)ne10, (uint32_t)ne10, (uint32_t)ne01,
             (uint32_t)(ne00 * ne01), stride_batch_y, (uint32_t)(ne20 * ne21),
             fusion_flags,
-            (uint32_t)nei0, (uint32_t)ne11, expert_i1, nbi1
+            (uint32_t)nei0, (uint32_t)ne11, expert_i1, nbi1,
+            (uint32_t)ne02
         };
         ggml_vk_dispatch_pipeline(ctx, subctx, dmmv,
             {
@@ -11924,6 +11929,7 @@ static void ggml_vk_add_id(ggml_backend_vk_context * ctx, vk_context& subctx, co
         (uint32_t)src0->nb[2] / src0_type_size,
         (uint32_t)src1->nb[1] / src1_type_size,
         (uint32_t)src2->nb[1] / src2_type_size,
+        (uint32_t)src1->ne[1],
     });
 }
 
@@ -12863,6 +12869,29 @@ static void ggml_vk_topk_moe(ggml_backend_vk_context * ctx, vk_context& subctx,
         pc.output_bias = 0.0f;
     }
 
+    const ggml_tensor * argsort_node;
+    switch (mode) {
+    case TOPK_MOE_EARLY_SOFTMAX_NORM:
+    case TOPK_MOE_EARLY_SOFTMAX:
+        argsort_node = cgraph->nodes[node_idx + 2];
+        break;
+    case TOPK_MOE_SIGMOID_NORM_BIAS:
+        argsort_node = cgraph->nodes[node_idx + 3];
+        break;
+    case TOPK_MOE_LATE_SOFTMAX:
+        argsort_node = cgraph->nodes[node_idx + 0];
+        break;
+    default:
+        GGML_ABORT("unsupported topk_moe_mode");
+    }
+    if (n_rows <= 8) {
+        pc.ser_thresh      = ggml_get_op_params_f32(argsort_node, 2);
+        pc.ser_min_experts = ggml_get_op_params_i32(argsort_node, 1);
+    } else {
+        pc.ser_thresh      = 0.0f;
+        pc.ser_min_experts = 0;
+    }
+
     GGML_ASSERT(n_expert_used <= n_experts);
 
     const uint32_t rows_per_block = 4;
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/add_id.comp b/ggml/src/ggml-vulkan/vulkan-shaders/add_id.comp
index 495249d5f..22def8daa 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/add_id.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/add_id.comp
@@ -12,6 +12,7 @@ layout (push_constant) uniform parameter
     uint s02;
     uint s11;
     uint s21;
+    uint n_experts;
 } p;
 
 #define BLOCK_SIZE 512
@@ -29,6 +30,16 @@ void main() {
 
     const uint i11 = data_c[i1 + i2 * p.s21];
 
+    if (i11 >= p.n_experts) {
+        const uint s1_zero = p.ne0;
+        const uint s2_zero = p.ne0 * p.ne1;
+        const uint d0_zero = i1 * s1_zero + i2 * s2_zero;
+        for (uint i0 = gl_LocalInvocationID.x; i0 < p.ne0; i0 += BLOCK_SIZE) {
+            data_d[d0_zero + i0] = D_TYPE(0.0f);
+        }
+        return;
+    }
+
     const uint s1 = p.ne0;
     const uint s2 = p.ne0 * p.ne1;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/get_rows.comp b/ggml/src/ggml-vulkan/vulkan-shaders/get_rows.comp
index e88bdd057..4c71c65e7 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/get_rows.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/get_rows.comp
@@ -22,6 +22,13 @@ void main() {
 
             const uint i01 = data_b[get_boffset() + i10*p.nb10 + i11*p.nb11 + i12*p.nb12];
 
+            if (i01 == 0xFFFFFFFFu) {
+                const uint d_offset_zero = get_doffset() + i10*p.nb21 + i11*p.nb22 + i12*p.nb23;
+                data_d[d_offset_zero + i00] = D_TYPE(0.0f);
+                gid_y += gl_WorkGroupSize.y * gl_NumWorkGroups.y;
+                continue;
+            }
+
             const uint a_offset = get_aoffset() + i01*p.nb01 + i11*p.nb02 + i12*p.nb03;
             const uint d_offset = get_doffset() + i10*p.nb21 + i11*p.nb22 + i12*p.nb23;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/get_rows_quant.comp b/ggml/src/ggml-vulkan/vulkan-shaders/get_rows_quant.comp
index 9dba437ed..49e644716 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/get_rows_quant.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/get_rows_quant.comp
@@ -37,6 +37,13 @@ void main() {
             const uint iybs = i00 - i00%QUANT_K; // dst block start index
             const uint y_offset = QUANT_R == 1 ? 1 : QUANT_K/2;
 
+            if (i01 == 0xFFFFFFFFu) {
+                data_d[d_offset + iybs + iqs           ] = D_TYPE(0.0f);
+                data_d[d_offset + iybs + iqs + y_offset] = D_TYPE(0.0f);
+                gid_y += gl_WorkGroupSize.y * gl_NumWorkGroups.y;
+                continue;
+            }
+
             vec2 v = dequantize(ib, iqs, 0);
             const vec2 dm = get_dm(ib, 0);
             v = v * dm.x + dm.y;
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec.comp
index 5a9d0e778..4cb6403c9 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec.comp
@@ -141,7 +141,16 @@ void iter_aligned_nonquant(inout FLOAT_TYPE temp[NUM_COLS][NUM_ROWS], const uint
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     const uint tid = gl_LocalInvocationID.x;
 
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (tid == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
     const bool is_aligned_nonquant =
         p.batch_stride_b % 4 == 0 && b_offset % 4 == 0 &&
         p.ncols % 4 == 0 && BLOCK_SIZE % 4 == 0 &&
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_base.glsl b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_base.glsl
index 4aeda68c7..88a65a40a 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_base.glsl
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_base.glsl
@@ -31,6 +31,7 @@ layout (push_constant) uniform parameter
     uint ne11;
     uint expert_i1;
     uint nbi1;
+    uint n_experts;
 #else
     uint base_work_group_y;
     uint ne02;
@@ -44,7 +45,7 @@ layout (push_constant) uniform parameter
 uint expert_id;
 #endif
 
-void get_offsets(out uint a_offset, out uint b_offset, out uint d_offset) {
+bool get_offsets(out uint a_offset, out uint b_offset, out uint d_offset) {
 #ifdef MUL_MAT_ID
     const uint expert_i0 = gl_WorkGroupID.y;
 #else
@@ -84,6 +85,13 @@ void get_offsets(out uint a_offset, out uint b_offset, out uint d_offset) {
 #else
             batch_idx * p.batch_stride_d;
 #endif
+
+#ifdef MUL_MAT_ID
+    if (expert_id >= p.n_experts) {
+        return false;
+    }
+#endif
+    return true;
 }
 
 layout (constant_id = 0) const uint BLOCK_SIZE = 32;
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_m.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_m.comp
index 3ea24a76c..031dd0f16 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_m.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_m.comp
@@ -93,7 +93,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint ib32,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_s.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_s.comp
index fd953c8fa..f5b66b66a 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_s.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq1_s.comp
@@ -56,7 +56,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint ib32,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_s.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_s.comp
index b4f6d1d6b..76f2b4d6e 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_s.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_s.comp
@@ -51,7 +51,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xs.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xs.comp
index d8dafe5f7..4f40fffe5 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xs.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xs.comp
@@ -66,7 +66,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xxs.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xxs.comp
index f75dcf833..5898eeb54 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xxs.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq2_xxs.comp
@@ -48,7 +48,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_s.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_s.comp
index 5cdf2a89d..fd65f5f7f 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_s.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_s.comp
@@ -51,7 +51,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint ib32,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_xxs.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_xxs.comp
index a88898109..ad4e8461c 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_xxs.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_iq3_xxs.comp
@@ -49,7 +49,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q2_k.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q2_k.comp
index 975cec801..5f64b8200 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q2_k.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q2_k.comp
@@ -80,7 +80,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q3_k.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q3_k.comp
index 93e48b790..89a12f805 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q3_k.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q3_k.comp
@@ -75,7 +75,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint ix, co
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q4_k.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q4_k.comp
index 93fbacc62..146d1f392 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q4_k.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q4_k.comp
@@ -86,7 +86,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint v_im,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q5_k.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q5_k.comp
index 54d7e1bcd..b9d996585 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q5_k.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q5_k.comp
@@ -118,7 +118,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint v_im,
 
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q6_k.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q6_k.comp
index 3e89d91cb..0062561cb 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q6_k.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vec_q6_k.comp
@@ -78,7 +78,16 @@ void calc_superblock(const uint a_offset, const uint b_offset, const uint itid,
 
 void compute_outputs(const uint first_row, const uint num_rows) {
     uint a_offset, b_offset, d_offset;
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (gl_LocalInvocationID.x == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
 
     const uint num_blocks_per_row = p.ncols / QUANT_K;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
index 7bbee577f..6a2d8b8b1 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/mul_mat_vecq.comp
@@ -76,7 +76,16 @@ void iter(inout FLOAT_TYPE temp[NUM_COLS][NUM_ROWS], const uint first_row, const
 void compute_outputs(const uint32_t first_row, const uint32_t num_rows) {
     const uint tid = gl_LocalInvocationID.x;
 
-    get_offsets(a_offset, b_offset, d_offset);
+    if (!get_offsets(a_offset, b_offset, d_offset)) {
+        if (tid == 0) {
+            [[unroll]] for (uint j = 0; j < NUM_COLS; ++j) {
+                [[unroll]] for (uint n = 0; n < num_rows; ++n) {
+                    data_d[j*p.batch_stride_d + d_offset + first_row + n] = D_TYPE(0.0f);
+                }
+            }
+        }
+        return;
+    }
     a_offset *= QUANT_K / QUANT_K_Q8_1;
     b_offset /= QUANT_K_Q8_1;
 
diff --git a/ggml/src/ggml-vulkan/vulkan-shaders/topk_moe.comp b/ggml/src/ggml-vulkan/vulkan-shaders/topk_moe.comp
index ef2f202ec..182c26c0e 100644
--- a/ggml/src/ggml-vulkan/vulkan-shaders/topk_moe.comp
+++ b/ggml/src/ggml-vulkan/vulkan-shaders/topk_moe.comp
@@ -23,6 +23,8 @@ layout (push_constant) uniform parameter
     uint with_norm;
     float output_scale;
     float output_bias;
+    float ser_thresh;
+    uint ser_min_experts;
 };
 
 layout(local_size_x_id = 0, local_size_y = 4, local_size_z = 1) in;
@@ -142,6 +144,8 @@ void main() {
 
     float wt_sum = 0.f;
 
+    float first_max_val = 0.0f;
+
     float output_weights[experts_per_thread];
 
     [[unroll]]
@@ -176,15 +180,33 @@ void main() {
             }
         }
 
+        if (k == 0) {
+            first_max_val = max_val;
+        }
+
+        bool ser_pruned = false;
+        if (ser_thresh > 0.0f && k >= int(ser_min_experts)
+            && gating_func != GATING_FUNC_SOFTMAX_WEIGHT) {
+            if (max_val < ser_thresh * first_max_val) {
+                ser_pruned = true;
+            }
+        }
+
         if ((k & (WARP_SIZE - 1)) == lane) {
-            output_weights[k / WARP_SIZE] = max_val;
+            if (!ser_pruned) {
+                output_weights[k / WARP_SIZE] = max_val;
+            }
         }
 
         if ((max_expert & (WARP_SIZE - 1)) == lane) {
             selection_probs[max_expert / WARP_SIZE] = -INFINITY;
 
-            ids[ids_offset + k] = max_expert;
-            wt_sum += max_val;
+            if (ser_pruned) {
+                ids[ids_offset + k] = 0xFFFFFFFFu;
+            } else {
+                ids[ids_offset + k] = max_expert;
+                wt_sum += max_val;
+            }
         }
     }
 
